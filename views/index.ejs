<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Bienvenido</title>
  <style>
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background: linear-gradient(135deg, #667eea, #764ba2);
      color: white;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      height: 100vh;
      margin: 0;
    }
    .card {
      background: rgba(255, 255, 255, 0.1);
      border-radius: 12px;
      padding: 2rem 3rem;
      box-shadow: 0 8px 32px 0 rgba(31, 38, 135, 0.37);
      text-align: center;
      max-width: 400px;
    }
    h1 {
      margin-bottom: 1rem;
      font-weight: 700;
    }
    p {
      font-size: 1.2rem;
      margin: 0.5rem 0;
    }
    .contador {
      margin-top: 1rem;
      font-size: 1rem;
      font-weight: 600;
      color: #ffd700;
    }
  </style>
</head>
<body>
  <div class="card">
    <h1><%= mensaje %></h1>
    <% if (!esNuevo) { %>
      <p class="contador">Has visitado esta página <%= conteo %> veces.</p>
    <% } else { %>
      <p>¡Bienvenido a nuestra web!</p>
    <% } %>
  </div>
</body>
</html>
